repos:
-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.10  # Use the latest version available
    hooks:
    -   id: ruff-check
        args: [ --fix ]
        files: src/
    -   id: ruff-format
        files: src/